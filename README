В датасете есть набор данных размера 616*448, со столбцами вида: 
unnamed 0 (содержащий номера строк), im (который необходимо предсказать, может принимать 2 значения: 1.0 или 0.0) и столбцы с названиями v1,v2,v3...,v446
В файле model содержится реализация ансамбля моделей машинного обучения.
В results_without содержатся метрики этой модели без использования scale_pos_weight.
В results_with содержатся метрики этой модели с использованием scale_pos_weight.
Датасет из приведённого задания не может быть выложен по некоторым причинам, однако полезно будет заметить, что классы в нём несбалансированны.
